mixin rate(numberFillStars)

  div.rate
    each value,index in Array(5)
      if index <= numberFillStars - 1
        img.rate__img(src=require('./blocks/rate/icon-star.svg'))
      else
        img.rate__img(src=require('./blocks/rate/icon-star-border.svg'))

